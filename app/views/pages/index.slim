- if user_signed_in?
    h1.text-3xl.font-bold.underline = "You are signed in #{current_user.email}"
    = link_to "Sign Out", destroy_user_session_path, data:{turbo_method: :delete,
    turb_confirm:"Continue to Sign Out"}
- else
    h1 The home page

